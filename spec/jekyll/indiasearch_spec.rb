require "spec_helper"

RSpec.describe Jekyll::Indiasearch do
  it "has a version number" do
    expect(Jekyll::Indiasearch::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
